database:
  etcd:
    protocol: "http"
    request_timeout: 10
    connections:
      - host: 0.0.0.0
        port: 2379

schemas:
  panels_path: "schemas/panels"
  queries_path: "schemas/queries"
  interval: "5m"
readonly: true
