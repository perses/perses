database:
  etcd:
    protocol: "http"
    request_timeout: 10
    connections:
      - host: 0.0.0.0
        port: 2379

schemas:
  charts_path: "schemas/charts"
  queries_path: "schemas/queries"
  interval: "5m"
