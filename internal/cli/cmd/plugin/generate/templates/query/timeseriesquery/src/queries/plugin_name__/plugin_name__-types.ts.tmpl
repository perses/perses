import type { DatasourceSelector, TimeSeries } from '@perses-dev/spec';

export interface {{ .PluginPascalName }}Spec {
  query: string;
  datasource?: DatasourceSelector;
}

// TODO: import this type from your datasource or an existing datasource plugin
export type DatasourceQueryResponse = {
  status: string;
  data: Array<Pick<TimeSeries, 'name' | 'values'>>;
  warnings?: string[];
};
