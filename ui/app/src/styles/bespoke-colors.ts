/**
 * Some colors that we use in a limited number of places in the application
 * that we should decide to either make part of the theme under some sort of
 * semantic/meaningful palette keys or convert the usages to something else.
 */
export const BespokeColors = {
  MEDIUM_BLUE: '#0011DE',
  BLACK_PEARL: '#151920',
} as const;

/**
 * Basically grey 900, 700, 500, 300, and 100, only slightly blue shifted.
 */
export const BespokeGreys = {
  EBONY_CLAY: '#2A2E3E',
  COMET: '#676B7D',
  SANTAS_GRAY: '#A1A6B9',
} as const;
